sudo apt install ros-<distro>-azure-kinect-ros-driver
roslaunch azure_kinect_ros_driver driver.launch
rostopic list

Start the Kinect: ros2 launch azure_kinect_ros_driver driver.launch.py

Verify the Topics: ros2 topic list (Look for /rgb/image_raw)

Run your Node: python3 your_script_name.py

source /opt/ros/humble/setup.bash
# And if your custom services are in a workspace:
# source ~/your_workspace/install/setup.bash

pip install ultralytics

# Optional voice query support. The code still works with typed input if these are not installed.
# Try this first:
pip install SpeechRecognition pyttsx3 pyaudio

# If pyaudio fails on Ubuntu/ROS, install the system audio packages, then try pip again:
sudo apt update
sudo apt install portaudio19-dev python3-pyaudio
pip install SpeechRecognition pyttsx3 pyaudio

# Quick voice library test:
python3 -c "import speech_recognition, pyttsx3, pyaudio; print('voice libs ready')"

sudo apt update
sudo apt install ros-humble-cv-bridge ros-humble-vision-msgs ros-humble-message-filters
